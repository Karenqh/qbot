<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="nanqineveryday.qbot.LoginActivity">

    <TextView
        android:text="@string/login_title"
        android:textSize="35sp"
        android:gravity="center"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="19dp"
        android:textColor="@color/pn_blue"
        android:id="@+id/logintitleText"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <EditText
        android:hint="@string/robotname_hint"
        android:inputType="text"
        android:layout_width="match_parent"
        android:layout_height="75dp"
        android:textSize="30sp"
        android:textColor="#333"
        android:gravity="center"
        android:id="@+id/login_robotname"
        android:layout_marginTop="38dp"
        android:layout_below="@+id/logintitleText"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <EditText
        android:hint="@string/username_hint"
        android:inputType="text"
        android:layout_width="match_parent"
        android:layout_height="75dp"
        android:textSize="30sp"
        android:textColor="#333"
        android:gravity="center"
        android:id="@+id/login_username"
        android:layout_marginTop="22dp"
        android:layout_below="@+id/login_robotname"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <Button
        android:id="@+id/loginbutton"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:text="@string/login_submit"
        android:textSize="25sp"
        android:textColor="#FFF"
        android:background="@color/pn_red"
        android:onClick="joinChat"
        android:layout_below="@+id/login_username"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="20dp" />

</RelativeLayout>